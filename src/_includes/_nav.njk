<nav aria-label="Main Navigation">
  <ul class="nav">
    <li class="has-submenu nav-button nav-button" id="film">
      film
      <ul class="submenu">
        {% for page in collections.film %}
          <li><a href="{{ page.url | url }}">{{ page.data.title }}</a></li>
        {% endfor %}
      </ul>
    </li>
    <li class="has-submenu nav-button" id="movement">
      movement
      <ul class="submenu">
        {% for page in collections.movement %}
          <li><a href="{{ page.url | url }}">{{ page.data.title }}</a></li>
        {% endfor %}
      </ul>
    </li>
    <li class="has-submenu nav-button" id="creators">
      creators
      <ul class="submenu">
        {% for page in collections.creators %}
          <li><a href="{{ page.url | url }}">{{ page.data.title }}</a></li>
        {% endfor %}
      </ul>
    </li>
    <li class="nav-button" id="connect"><a href="/connect">connect</a></li>
  </ul>
</nav>
